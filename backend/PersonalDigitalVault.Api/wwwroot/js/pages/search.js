import { requireAuth } from '../auth/authGuard.js'; import { searchApi } from '../api/searchApi.js'; requireAuth(); const f = document.querySelector('form'), list = document.getElementById('list'); f.onsubmit = async e => { e.preventDefault(); const rows = await searchApi.search(f.keyword.value); list.innerHTML = rows.map(x => `<div class="card">${x.type}: ${x.title}</div>`).join(''); };
