import{requireAuth}from'../auth/authGuard.js';import{credentialApi}from'../api/credentialApi.js';requireAuth();const list=document.getElementById('list'),f=document.querySelector('form');async function load(){const rows=await credentialApi.all();list.innerHTML=rows.map(x=>`<div class="card"><b>${x.title}</b><div>${x.username}</div><div>${x.password}</div></div>`).join('');}f.onsubmit=async e=>{e.preventDefault();await credentialApi.create({title:f.title.value,username:f.username.value,password:f.password.value,website:f.website.value,notes:f.notes.value});f.reset();load();};load();
