import{requireAuth}from'../auth/authGuard.js';import{documentApi}from'../api/documentApi.js';import{fileSize}from'../utils/formatters.js';requireAuth();const list=document.getElementById('list'),f=document.querySelector('form');async function load(){const rows=await documentApi.all();list.innerHTML=rows.map(x=>`<div class="card vault-item"><span>${x.fileName} · ${fileSize(x.fileSize)}</span><button data-id="${x.id}" data-name="${x.fileName}">Download</button></div>`).join('');list.querySelectorAll('button').forEach(b=>b.onclick=()=>documentApi.download(b.dataset.id,b.dataset.name));}f.onsubmit=async e=>{e.preventDefault();const data=new FormData();data.append('file',f.file.files[0]);if(f.folderId.value)data.append('folderId',f.folderId.value);await documentApi.upload(data);f.reset();load();};load();
