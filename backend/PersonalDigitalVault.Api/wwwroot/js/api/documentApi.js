import{api}from'./apiClient.js';import{API_BASE_URL}from'../config.js';import{tokenManager}from'../auth/tokenManager.js';
export const documentApi={all:()=>api('/documents'),get:id=>api(`/documents/${id}`),upload:form=>api('/documents/upload',{method:'POST',body:form}),rename:(id,d)=>api(`/documents/${id}`,{method:'PUT',body:JSON.stringify(d)}),remove:id=>api(`/documents/${id}`,{method:'DELETE'}),download:async(id,fileName)=>{const r=await fetch(`${API_BASE_URL}/documents/${id}/download`,{headers:{Authorization:`Bearer ${tokenManager.get()}`}});if(!r.ok)throw new Error('Download failed');const blob=await r.blob();const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=fileName;a.click();URL.revokeObjectURL(url);}};
